#!/bin/bash

# Code generated by template::sh_deploy_all.sh.jinja; DO NOT EDIT.

set -euo pipefail -o posix

cd "$(dirname "$0")/../../"

set -- "$@" --refresh
{##}

{%- for file in pathlib.Path('../deploy/.src').glob('*.zml') | list | sort(attribute='stem') %}
{%- if not file.stem.startswith('xxx_') %}
./deploy/sh/{{ file.stem }} "$@"
{%- endif %}
{%- endfor %}
