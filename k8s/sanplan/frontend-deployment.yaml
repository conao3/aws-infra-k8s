apiVersion: apps/v1
kind: Deployment
metadata:
  name: sanplan-frontend
  labels:
    app: sanplan-frontend
spec:
  replicas: 1
  selector:
    matchLabels:
      app: sanplan-frontend
  template:
    metadata:
      labels:
        app: sanplan-frontend
    spec:
      imagePullSecrets:
      - name: ecr-secret
      containers:
      - name: nginx
        image: ${AWS_ACCOUNT_ID}.dkr.ecr.ap-northeast-1.amazonaws.com/${PREFIX}-sanplan-frontend:latest
        ports:
        - containerPort: 80
        readinessProbe:
          httpGet:
            path: /
            port: 80
          initialDelaySeconds: 2
          periodSeconds: 10
