apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: app3
spec:
  ingressClassName: traefik
  rules:
  - host: app3.sancode.dev
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: app3
            port:
              number: 80
