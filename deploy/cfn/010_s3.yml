# Code generated by Makefile. DO NOT EDIT.

AWSTemplateFormatVersion: '2010-09-09'
Transform: AWS::Serverless-2016-10-31
Parameters:
  Prefix:
    Type: String
  Env:
    Type: String
Resources:
  BucketScript:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ${Prefix}-script-${AWS::AccountId}
  BucketBackup:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ${Prefix}-backup-${AWS::AccountId}
  BucketImageBuilder:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ${Prefix}-image-builder-${AWS::AccountId}
  BucketWafLog:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: aws-waf-logs-${Prefix}-${AWS::AccountId}
  BucketAthenaResult:
    Type: AWS::S3::Bucket
    Properties:
      BucketName:
        Fn::Sub: ${Prefix}-athena-result-${AWS::AccountId}
Outputs:
  BucketScript:
    Value:
      Ref: BucketScript
    Export:
      Name:
        Fn::Sub: ${Prefix}-BucketScript
  BucketBackup:
    Value:
      Ref: BucketBackup
    Export:
      Name:
        Fn::Sub: ${Prefix}-BucketBackup
  BucketImageBuilder:
    Value:
      Ref: BucketImageBuilder
    Export:
      Name:
        Fn::Sub: ${Prefix}-BucketImageBuilder
  BucketWafLog:
    Value:
      Ref: BucketWafLog
    Export:
      Name:
        Fn::Sub: ${Prefix}-BucketWafLog
  BucketAthenaResult:
    Value:
      Ref: BucketAthenaResult
    Export:
      Name:
        Fn::Sub: ${Prefix}-BucketAthenaResult
